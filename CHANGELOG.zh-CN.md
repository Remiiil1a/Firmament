# 更新日志

**Firmament（edit of coderbot's Steadfast）** 每个版本改了什么。面向
**Minecraft: Java 版**、为 **Iris + Sodium** 制作的光影包。这里是简版；
一版一份的详细日志是 `RELEASE_NOTES-*.md`，保存在项目里，不随包发行。

> Steadfast is free and open-source software developed by coderbot, and can be
> downloaded from https://modrinth.com/shader/steadfast-shaders (Modrinth),
> https://www.curseforge.com/minecraft/shaders/steadfast (CurseForge), or
> https://github.com/coderbot16/Steadfast (GitHub). Anyone can modify and
> distribute it under the terms of the GNU General Public License, version 3.

（上面这段是 Steadfast 附加条款要求逐字给出的声明原文，故保留英文。）

**本包不是 Steadfast 的官方发行版，coderbot 不为它提供支持。** 有问题请提到本项目，
不要提到 Steadfast 的仓库或发布页。

---

## v0.6 - 2026-09-26

### 新增

* **辉光（Bloom）。** 比显示器能表示的更亮的地方，现在会在周围洒出一圈光晕；
  强度、开始起作用的亮度、以及光晕铺开的半径都是设置项。**表面的高光默认被排除
  在外**：高光是"一张关于光的画"，而不是光本身，把它也 bloom 一遍，等于给每块
  湿石头都加上第二个太阳。
* **体积雾。** 空气变成了**要被步进穿过的介质**，而不是盖在画面上的一层色。它按
  画面的**四分之一分辨率**绘制，并由阴影贴图点亮——所以树冠漏下来的光柱是**它**
  投出来的；介质本身是一团锁在世界坐标里的噪声，随云用的同一阵风漂移，因此它
  **不会跟着镜头滑动**。浓度、高度、基准、距离、步进次数、噪声的强度／尺度／
  倍频都是设置项，也可以按整帧分辨率绘制；下雨会让它变厚。
* **雨与雪粒子。** 在不换贴图的前提下贴图允许的两个控制：**每一格雨柱里横向平铺
  几次**，以及**每颗雨滴保留原宽度的多少**（后者决定雨丝粗细）。颜色也可以抽掉
  或推浓。雪由同一条程序绘制，跟着一起变。
* **水里的日月用的是游戏自己的贴图。** 包内带着 `sun.png` 与八张月相并反射它们，
  所以水面上的那个日轮就是天上显示的那个，月相也是天上正在显示的那个。两个天体
  各有一项**对着天空的尺寸微调**；它们**绕哪根轴转**也是一个独立设置项——因为那
  根轴既不是世界上方向、也不是地平线：日月东升西落，而轨迹是**斜**的。

### 更改

* **时间抗锯齿默认关闭**，并移到了开发页作为实验性效果。它能产生的黑团至今没有
  解释，而会产生黑团的效果不该待在默认值里。
* **出厂默认值重新调过**：辉光取消了阈值，PBR 自发光更亮，雨更细，体积雾更薄且
  对下雨的反应更弱，水面反射的天空更少，而水面上的日月更亮。

## v0.5 - 2026-09-22

### 新增

* **彩色阴影。** 透过染色玻璃的阳照落到地面时带的是玻璃自己的颜色，而不是在光里
  留一个洞；地狱传送门则用它自己的辉光给周围的光上色。颜色取自玻璃自己的贴图，
  所以重画过玻璃颜色的资源包会连带把光也一起改色。带多少颜色是一个设置项；
  传送门辉光单独一个开关，并且**有意不落在半透明表面上**，这样传送门不会从内部
  照亮自己的面。
* **体积光总强度**（相对于本包原本画出来的量），并且**水下也画光柱**了。水下光柱与
  水面上共用同一根光轴，在水面波纹把光聚起来的地方更亮，并且在总强度之上还有
  一个自己的倍率。
* **附魔光效强度。** 附魔物品与盔甲上的闪光可以调亮。游戏是以"叠加自身颜色"的方式
  画这层的，所以它贡献的光按设置值的平方增长。
* **云内多次散射**，散射次数与每次的衰减都是设置项。
* **反射相关的设置**：金属反射强度、反射所需的光滑度、粗糙表面的反射铺开多远，
  以及一个"只显示反射"的调试视图。
* **平滑视差**：高度通道是跨纹素插值，还是只取纹素中心，做成一个开关。
* **水里的星空**：星星和日月一样会映在水面上。

### 变更

* **反射整条重做。** 反射线在离开表面时不再朝法线掰；追踪的步数预算翻倍；
  粗糙表面的反射改在**它自己的粗糙度张开的那一圈**上取样（两圈、每圈四个方向），
  它反射的天空也按同一圈取样，而不是两者都只取一个方向了事。
* **视差行进重写**，位移的方向改由**贴图自己的 UV 轴**给出，而不是来自几何体的
  切线基——这正是它在某些面上朝反方向走的原因。位移现在被限制在片元自己所属的
  sprite 之内（先按片元剩下的余地淡出，再在边界处夹住），此前会绕到贴图的另一侧。
* **出厂默认值有调整**：天空反射默认关闭、体积光是本包原本量的两倍、水下光柱
  是水面上光柱的 1.5 倍、环境光下限调低。

### 修复

* **金属方块上的反射会不全或变形。** 追踪器里有一处除零，使它的厚度上限整个失效，
  于是隔着任意厚的实体也会判成命中。
* **隔着水、冰或玻璃的像素，反射的起点算错了。**
* **隔着水看一个方块会看到两个像**——一个被水面折射过，另一个还留在方块原处。
  现在隔着水、冰或玻璃看到的表面一律不给环境反射；位置画错的反射比没有反射更难看。
* **带斜边的方块（多数资源包里的金属块，最外圈纹素有一道倒角）边缘那一圈天空**已修掉。
  反射线从那些纹素离开时会拐回自己刚离开的表面，那一击被当成"打到自己"丢掉，
  于是退回天空。
* **第三人称下，手里拿的东西与身上的盔甲会透出背后的世界。**
* **手持物品曾被一层指向玩家身后的天空反射盖住**，金属因此发白。那一项已删除，
  改为真的去追踪反射。
* **视差会闪**，而且普通方块面上的细颗粒查出来是"高度通道一个纹素一个纹素地读"
  ——上面那个插值开关就是为此而设。位移里还有一次多余的除法，那正是它在某些面上
  冲过头的原因。
* **视差的位移方向是反的**：位移按几何体的切线基铺开，而不是按贴图自己的 UV 轴。

### 移除

* **水散射**（v0.4 加入的）：选项与背后的代码都已删除。水的吸收、水色与反射不变。
* **附魔闪光染色**试过之后删掉了。原版闪光本来就是附魔的颜色，而且是在本包看到
  这一层之前就乘进去的，所以再乘一次颜色只可能把光拿走、而不是换色——实测正是如此。

---

## v0.4 - 2026-09-20

### 新增

* **水面按"这个面自己的切线基"铺开**，而不是按假定的世界轴。水的颜色、吸收、散射
  与反射都没有变；与世界轴对齐的面上看不出区别，区别出现在不对齐的那些面上。
* **水散射**，水页面上的三个选项。默认全关。
* **本包自己生成的星空**，亮度、分辨率、密度与星星大小都可调。它随入夜淡入，
  下雨时被盖住。原版的星空仍在画，且未改动。
* **水里映出太阳和月亮**，大小与游戏自己的一致，月亮是太阳的五十分之一；
  只在水里——玻璃与冰保留天空反射与它们自己的菲涅尔项。
* **屏幕空间阴影的强度**，以及日月交接：在贴着地平线的两度范围内渐变过渡，
  而不是直接切换。
* **云的相位、透射与层高衰减**做成选项。
* **设置菜单重排成两级**，每一项都能找到，出厂预置在中英文里叫同一个名字。

### 修复

* **雷雨天里天空会突然发白几秒。** 用来识别游戏星空四边形的那条判据，在三个通道
  数值相等时也会命中游戏自己画的天色四边形，而雨天正好把天色推到这个值上。
* **屏幕空间阴影把射线后面的表面也算成了遮挡物**，与"阴影"正好相反；它还会随步数
  一起变。
* **水面的视差映射在原版渲染距离边缘停住**，因此本包的水与远景渲染器的水相接处
  不再出现台阶。
* **日月交接时云层不再跳变**，此前它会通过相位与透射这两项跳一下。
* **出厂预置不再在英文菜单里显示中文。**
* **本来有多个取值、却渲染成"点一下跳一档"的选项改回滑杆。**

---

## v0.3 - 2026-09-18

### 新增

* **给阴影贴图够不到的远景补上屏幕空间阴影**，交接点取"阴影距离"与"视距"中较小的
  那个。Distant Horizons 与 Voxy 的地形——以及所有画在那个距离之外的东西——此前
  只被太阳照亮、不投任何阴影；现在会投影，且不会有任何东西被投两次。
* **时间抗锯齿（TAA），默认开启。** 历史权重按像素移动的距离变化：静止时积累长历史、
  把噪点压掉，走动时只留短历史、不拖影。本包自己的噪点每帧都会移动，
  这样才有东西可平均。
* **不是平铺在地面的水**——瀑布侧面、沿坡流下的水——按水来绘制，而不是当成平贴图：
  它有自己的波面、与湖面同一套吸收模型给出的水色，以及按粗糙度滤波的反射。

### 修复

* **Voxy 的光影补丁。** `voxy.json` 是用旧格式写的，导致整个文件加载失败：
  补丁里的 uniform 一个都没声明，远景地形完全没有被本包着色。

---

## v0.2 - 2026-09-14

### 新增

* **由立方体云格组成的云层**，替代原版的扁平方块云；它缓慢跟随本包的天气变化，
  并与平面云用同一股风飘动。只出现在主世界；不会给地面投影。
* **动态模糊**，沿相机自己的位移方向，有意把走路摇晃排除在外；模糊量与采样数可调。
  默认关闭。
* **日月大小**：它们的高光带的是太阳真实的角直径，而不是一个像素。
* **设置菜单的"特效"页拆成子页**，未完成的 TAA 标注为实验性。

### 修复

* **云层不再出现在下界**，云自身的明暗也恢复成硬边。
* **玻璃与平静水面上的反射不再随走路摇晃发抖。**
* **手持物品不再透出背后的世界**——手部整块不再参与环境反射。
* **太阳高光**此前用错了量纲、几乎看不出变化；现在按半度阳光该有的样子铺开。

---

## v0.1 - 2026-09-14

第一个版本：材质那条线，也是第一次对外发布。

### 新增

* **labPBR 1.3 材质支持**：法线、高光/金属、材质环境光遮蔽、次表面散射、
  孔隙度/潮湿、自发光，并带逐通道的调试视图。
* **反射**：每个材质的天空反射，加上可选的、在延迟阶段计算的屏幕空间世界反射。
  染色玻璃与玻璃板有自己的镜面反射。
* **末地**：它自己的光照、虚空辉光与末地雾，以及判断维度的调试视图。
* **切线基取自几何体自带的切线**（`at_tangent`），而不是从深度里重建，
  修掉了法线贴图在远处翻转的问题。

---

## 不是 Steadfast 的官方发行版

按 Steadfast 的附加条款，修改版的名称必须以 `(edit of coderbot's Steadfast)` 结尾——
本包做到了：包名、文件名、以及设置菜单里的标题都是如此。coderbot 不为本包提供支持，
关于本包的问题请不要提交到 Steadfast 的 issue 区。
